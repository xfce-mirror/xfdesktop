	- if ~/.xfce4/menu.xml doesn't exist create it from default
	- Ask to save only if real changes
	- check if binary exists
	- use the status bar
	- permit choice of the colors
	- show which menu is edited in title bar
	- add startup notify tag support
	- add run in term tag support
